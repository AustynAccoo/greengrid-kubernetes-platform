# Network

Creates a custom-mode regional VPC subnet with Private Google Access and separate alias-IP ranges for nodes, Pods, and Services. It intentionally creates no NAT, VPN, load balancer, or unrelated firewall rules.

