# Project Services

Enables Compute Engine, GKE, Artifact Registry, IAM, IAM Credentials, Service Usage, Cloud Resource Manager for project IAM, and Cloud Logging/Monitoring for the configured GKE telemetry. `disable_on_destroy = false` prevents a stack teardown from disabling shared APIs and disrupting resources outside this state.
