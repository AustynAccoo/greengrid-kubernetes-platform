{{- if .Values.limitRange.enabled }}
apiVersion: v1
kind: LimitRange
metadata:
  name: {{ include "greengrid.fullname" . }}
  labels:
    {{- include "greengrid.labels" . | nindent 4 }}
spec:
  limits:
    - type: Container
      {{- toYaml .Values.limitRange.container | nindent 6 }}
{{- end }}

